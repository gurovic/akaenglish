—троки в Python задаютс€ кавычками или апострофами с двух сторон. —начала пишетс€ им€ строки (любое слово(а) на английском через У_Ф c маленькой буквы), а потом ее значение

ѕример:
first_string = УHello,Ф
second = Сworld!Т

----------„тобы ввести и вывести строки, нужно использовать команды input() и print() соответственно.

ѕример:
first_string = input()	#вводите строку, например один два три
second_string = У123Ф
print(second_string)
print(first_string, second_string)

ѕрограмма выведет:
123
один два три 123

----------“ак же строки можно складывать и умножать на какое-то число.

ѕример:
first_string = УqwertyФ
letter = УAФ
new_string = first_string + letter
print(letter * 5, new_string)

ѕрограмма выведет:
AAAAA qwertyA

----------ƒлина строки считаетс€ командой len().

ѕример:
first_string = УqwertyФ
print(len(first_string))

ѕрограмма выведет:
6

----------” каждого символа строки есть свой номер (индекс). Ѕлагодар€ этому можно выводить как и отдельные элементы, так и части строки (срезы).
s[0]	s[1]	s[2]	s[3]	s[4]	s[5]
  q	  w	  e	  r	  t	  y	
s[-9]	s[-8]	s[-7]	s[-6]	s[-5]	s[-4]	

ѕример:
s = УqwertyuioФ
print(s[1], s[-5])
print(s[0:5])

ѕрограмма выведет:
w t
qwert		#элементы c 0 по 5, т.е фактически с 1 по 4

----------ћетоды find, count, replace.
find(СТ) Ц находит данный элемент в строке и выводит его индекс.
count(СТ) Ц выводит количество данных элементов в строке.
replace(СТ, СТ) Ц замен€ет все первые элементы на вторые.

ѕример:
string = УPython languageФ
print(string.find(СlТ), string.find(СpТ))
print(string.count(СnТ)
print(string.replace(СPythonТ, С*Т))
print(string.replace(СТ, С$Т)

ѕрограмма выведет:
7 -1	#-1 т.к. символов УpФ нет в этой строке
2
